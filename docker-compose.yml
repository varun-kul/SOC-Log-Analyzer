# version: "3.9"

# services:
#   db:
#     image: postgres:16-alpine
#     environment:
#       POSTGRES_USER: soc_user
#       POSTGRES_PASSWORD: soc_pass
#       POSTGRES_DB: soc_db
#     ports:
#       - "5432:5432"
#     volumes:
#       - pg_data:/var/lib/postgresql/data
#     healthcheck:
#       test: ["CMD-SHELL", "pg_isready -U soc_user -d soc_db"]
#       interval: 5s
#       timeout: 5s
#       retries: 5

#   backend:
#     build: ./backend
#     ports:
#       - "8000:8000"
#     env_file:
#       - ./backend/.env
#     environment:
#       DATABASE_URL: postgresql://soc_user:soc_pass@db:5432/soc_db
#     volumes:
#       - ./backend:/app
#       - uploads:/app/uploads
#     depends_on:
#       db:
#         condition: service_healthy
#     command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

# volumes:
#   pg_data:
#   uploads:

version: "3.9"

services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: soc_user
      POSTGRES_PASSWORD: soc_pass
      POSTGRES_DB: soc_db
    ports:
      - "5432:5432"
    volumes:
      - pg_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U soc_user -d soc_db"]
      interval: 5s
      timeout: 5s
      retries: 5

  backend:
    build: ./backend
    ports:
      - "8000:8000"
    env_file:
      - ./backend/.env
    environment:
      DATABASE_URL: postgresql://soc_user:soc_pass@db:5432/soc_db
    volumes:
      - ./backend:/app
      - uploads:/app/uploads
    depends_on:
      db:
        condition: service_healthy
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000 --reload

  frontend:
    build: ./frontend
    ports:
      - "5173:5173"
    volumes:
      - ./frontend:/app
      - /app/node_modules
    depends_on:
      - backend

volumes:
  pg_data:
  uploads: